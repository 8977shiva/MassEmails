doctype html
html(lang='en')
  head
    title HOT LIST
    link(rel='stylesheet' href='https://cdn.jsdelivr.net/npm/semantic-ui@2.3.3/dist/semantic.min.css')
    style.
      table.ui.celled img { display: inline-block; }
      footer { margin: 35px 0 15px 0; text-align: center }
  body
    main#main
    h1.ui.center.aligned.header InnovaPath HOT LIST
    .ui.container
      table.ui.celled.table.center.aligned
       
          tr
            th Candidate Name
            th Current Location
            th Relocation
            th Introduction
          each std in val
            tr
              td #{std.name}
              td #{std.currentlocation}
              td #{std.relocation}
              td #{std.intro}
          
    footer
      p © #{new Date().getFullYear()} My Company